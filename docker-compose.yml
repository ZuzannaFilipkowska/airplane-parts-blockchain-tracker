version: '3.1'
services:
  database:
    image: 'postgres:15.2'
    ports:
      - '5432:5432'
    environment:
      - 'POSTGRES_USER=user'
      - 'POSTGRES_DB=parts'
      - 'POSTGRES_PASSWORD=pass'